/*
* Copyright (c)  ${YEAR}
* All rights reserved.
* ${DATE}
*/
package ${repositoryPackageName};

import org.springframework.stereotype.Repository;

/**
* 1.这里可以写基于StringQuery方式的自定义的接口，但是需要在实现类${entityFullName}RepositoryImpl当中进行实现，
* 2.如果使用Spring Data JPA语法，则写到${entityFullName}Repository接口当中
* 3.Service类注入接口时直接注入 ${entityFullName}Repository接口即可，当前接口不需要注入
* Created on ${DATE}
* @author ${USER}
* @since 2.1.0 by ueboot-generator
*/
@Repository
public interface ${entityName}BaseRepository  {

}